export default class TwoSum {

    private readonly numbers: number[];

    constructor(numbers: number[]) {
        this.numbers = numbers;
    }

    /**
     * This method will return an array with only two numbers (4 and 5 if the target is 9).
     */
    public target(sum: number): number[] {
        const twoSumNumbers: number[] = [];
        // TODO : populate twoSumNumbers with the two numbers.
        return twoSumNumbers;
    }

}